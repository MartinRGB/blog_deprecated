.copyright
    - var date = new Date();
    - var year = date.getFullYear();
    - year = year == theme.startyear ? year : theme.startyear + ' - ' + year;
    p Copyright © #{year} #[a(href=config.url)!= config.author]
